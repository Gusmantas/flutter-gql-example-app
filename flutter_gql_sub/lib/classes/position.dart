class Position {
  String type;
  DateTime timestamp;
  double latitude;
  double longitude;

  Position(
      {required this.type,
      required this.timestamp,
      required this.latitude,
      required this.longitude});
}
